---
import { SanityImage } from "~/components/Image";
const { src } = Astro.props;
---

<SanityImage
  client:only="react"
  src={src}
  width={1920}
  height={1080}
  className="w-full h-full object-cover"
/>
