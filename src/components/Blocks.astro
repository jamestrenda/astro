---
import type { Block as BlockProps } from "~/types/block";
import Block from "./Block.astro";

interface Props {
  blocks: BlockProps[];
}

const { blocks } = Astro.props;
---

{blocks.map((block) => <Block {...block} />)}
