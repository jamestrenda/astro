---
import dayjs from "dayjs";

interface Props {
  siteTitle: string;
}

const { siteTitle } = Astro.props;

const year = dayjs().format("YYYY");
---

<footer class="flex items-center justify-center py-4 text-sm bg-indigo-500">
  &copy; {year}
  {siteTitle}
</footer>
