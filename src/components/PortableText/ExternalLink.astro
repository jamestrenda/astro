---
// TODO: Add types

const props = Astro.props;

const { node, index, isInline, ...attrs } = props;
const { markDef } = node;
---

<a
  href={markDef.url ?? "#"}
  target={markDef.newWindow ? "_blank" : undefined}
  class="text-blue-500"
>
  <slot />
</a>
